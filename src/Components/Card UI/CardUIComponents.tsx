import React, { FunctionComponent } from 'react';
import styles from './_CardUIComponents.module.css'
interface OwnProps {}

type Props = OwnProps;

const CardUIComponents: FunctionComponent<Props> = (props) => {

  return (<div className={styles.CardUI}>

  </div>);
};

export default CardUIComponents;
